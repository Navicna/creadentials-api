// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource mongo {
  provider = "mongodb"
  url      = "mongodb+srv://victorcorassa:Master123@projeto-teste.etadrhx.mongodb.net/projeto-teste?retryWrites=true&w=majority"
}

model Users {
  id        String   @id @default(auto()) @map("_id") @mongo.ObjectId
  username  String
  password  String
  createdAt DateTime @default(now())
}
